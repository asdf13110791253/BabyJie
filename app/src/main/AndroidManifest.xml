<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_temp"
        android:roundIcon="@drawable/ic_launcher_temp"
        android:label="BabyJie"
        android:supportsRtl="true"
        android:theme="@style/Theme.BabyJie">

        <activity android:name=".SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".OrientationActivity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:taskAffinity="" />

        <!-- 重要：改回 mediaProjection，与已声明的权限匹配，确保服务能启动 -->
        <service
            android:name=".FloatWindowService"
            android:enabled="true"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:foregroundServiceType="mediaProjection" />

        <service
            android:name=".ScreenCaptureService"
            android:enabled="true"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:foregroundServiceType="mediaProjection" />

    </application>
</manifest>
